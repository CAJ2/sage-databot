query Search($query: String!, $types: [SearchType!], $limit: Int) {
  search(query: $query, types: $types, limit: $limit) {
    nodes {
      ... on Variant {
        id
        name
        desc
      }
      ... on Item {
        id
        name
        desc
      }
      ... on Component {
        id
        name
        desc
      }
      ... on Category {
        id
        label: name
        desc
        descShort
      }
      ... on Org {
        id
        label: name
        desc
        slug
      }
      ... on Place {
        id
        name
        desc
      }
      ... on Region {
        id
        name
        placetype
      }
      ... on Material {
        id
        name
        desc
      }
    }
  }
}
