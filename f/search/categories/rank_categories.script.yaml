summary: Compute rank for categories
description: Updates rank.qual, rank.pop, rank.order in public.categories
lock: '!inline f/search/categories/rank_categories.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    keys:
      type: array
      description: ''
      default: null
      items:
        type: string
      originalType: string[]
  required:
    - keys
