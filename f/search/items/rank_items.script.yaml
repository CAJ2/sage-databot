summary: Compute rank for items
description: Updates rank.qual, rank.pop, rank.order in public.items
lock: '!inline f/search/items/rank_items.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    keys:
      type: array
      description: ''
      default: null
      items:
        type: string
      originalType: string[]
  required:
    - keys
