summary: Compute rank for programs
description: Updates rank.qual, rank.pop, rank.order in public.programs
lock: '!inline f/search/programs/rank_programs.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    keys:
      type: array
      description: ''
      default: null
      items:
        type: string
      originalType: string[]
  required:
    - keys
