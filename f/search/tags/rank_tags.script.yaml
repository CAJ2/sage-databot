summary: Compute rank for tags
description: Updates rank.qual, rank.pop, rank.order in public.tags
lock: '!inline f/search/tags/rank_tags.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    keys:
      type: array
      description: ''
      default: null
      items:
        type: string
      originalType: string[]
  required:
    - keys
