summary: Compute rank for variants
description: Updates rank.qual, rank.pop, rank.order in public.variants
lock: '!inline f/search/variants/rank_variants.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    keys:
      type: array
      description: ''
      default: null
      items:
        type: string
      originalType: string[]
  required:
    - keys
